{
  "name": "hypercore-authenticate-session-extension",
  "version": "1.0.1",
  "description": "authenticate a hypercore replication session for a feed key",
  "scripts": {
    "test": "tape test/*.js"
  },
  "dependencies": {
    "hypercore-crypto": "^2.1.1"
  },
  "devDependencies": {
    "collect-stream": "^1.2.1",
    "hypercore-protocol": "^8.0.1",
    "random-access-memory": "^3.1.1",
    "tape": "^5.0.1",
    "to2": "^1.0.0"
  }
}
